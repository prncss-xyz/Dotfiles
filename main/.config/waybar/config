{
    "layer": "top",
    "position": "bottom",
    "modules-left": ["sway/workspaces",],
    "modules-center": [ "sway/window",],
    "modules-right": [ "sway/mode","cpu", "memory", "battery", "clock"],
    "sway/window": {
      "max-length": 100,
    },
    "cpu": {
      "interval": 10,
      "format": "{usage} · {load:0.01f} · {avg_frequency:0.01f}",
      "max-length": 10
    },
    "memory": {
      "format": "{avail:0.1f}"
    },
    "battery": {
      "format": "{time} {capacity}",
      "format-time": "{H}:{M}"
    },
    "clock": {
        "format": "{:%m-%d  %H:%M}"
    }
}
