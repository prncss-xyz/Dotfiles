#!/usr/bin/env sh

res=$(tr ',' '\t' <~/Personal/bookmarks.csv | fzf --delimiter='\t' --nth=1,2 --with-nth=1 | cut -f2)
if [ -n "$res" ]; then
  swaymsg exec .local/bin/browser "$res"
  # TODO: sway-mega-hotkey command
fi
