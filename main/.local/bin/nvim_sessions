#!/usr/bin/env sh

if [ -z "$1" ]; then
	if [ "$PWD" = "$HOME" ]; then
		nvim +"Telescope my_projects"
	else
		# FIXME:
    nvim
		# nvim +"lua require'bufjump'.local_backward()"
	fi
else
	nvim "$@"
fi
