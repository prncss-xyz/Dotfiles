#!/usr/bin/env sh

if [ -n "$1" ]; then
  res="$(fd --type file --follow --hidden --exclude .git --color=always -e "$1" | fzf)"
else
  res="$(fd --type file --follow --hidden --exclude .git --color=always | fzf)"
fi
if [ -n "$res" ]; then
  opener "$res"
fi
