#!/usr/bin/env sh
THEME=$(cat "$HOME/.config/theme/name")
FILE="$(shuf -n1 $HOME/Media/Pictures/wallpapers-lists/$THEME.txt)"
FILE="$HOME/Media/Pictures/wallpapers-lists/$FILE"
echo "output * bg \"$FILE\" fill" > ~/.config/sway/config.d/bg
